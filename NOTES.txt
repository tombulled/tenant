DONE:
    * Merging app/appset values is painful due to lists (info, sources, syncOptions, generators, goTemplateOptions) - the "objects" pattern
        Solution: just update the application-template.yaml file (SMALL) <-------------
        Solution2: Implement application extensions (MEDIUM/LARGE)
    * Can't disable apps created by an appset - generator selector (currently in root/values.yaml - done as x-git has no control over it?)
        Solution: Maybe do wrap the user's generators in a `merge` - provided merge inside merge is ok?
            > Combination-type generators (Matrix or Merge) can only be nested once.
        Solution2: Use an "extension" or some templating that always adds the selector on to every generator automatically <---------------------- (SMALL)
            NOTE: Needs to support both `generators` AND `generatorsObject`
        Solution3: Implement generatorsDefaults - pls no
        Solution4: Make use of applicationSetDefaults to always add a default "main" generator
        Solution5: Mandate one user-defined generator per appset?

Current outstanding problems:
    * Templating-self for resources - e.g. tenant/cluster/cluster-type etc.
        Solution: update build-resource-data helper (SMALL) <-------------------
    * Templating-self for application data (for use by appsets) - e.g. tenant/cluster/cluster-type etc.
        Solution: Hacky script as "extension" (LARGE)
        Solution2: Hacky script as block at the top of application-template.yaml (LARGE)
    * Merging type/cluster specific values for an app - the x-git generator (n.b. depends on templating-self)
        Solution1: this is cool functionality, but doesn't *need* to be enforced by the chart. solution 1 is remove it.
        Solution2: Implement this as an extension as is already done
            NOTE: Instead of the extensions pattern, implement this as a helper in _appset.tpl <------------------------------------------------ (LARGE)
        Solution3: Custom generator via plugin - noooo
        Solution4: customGenerators or some other field
 
    * Need additional resources (appprojects, namespaces)
        Soltion: just create them (SMALL)
    * Need additional namespace-specific resources
        Solution: create them and a helper (MEDIUM)

Unsolved problems:
    * Ordering of application sources in sourcesObject (NEED TO FIX)
    * Similarities between build-resource-data and application-template
    * Application resources get run through both build-resource-data and application-template (whereas appset apps don't)
    * Helpers should probably be prefixed with "tenant."

    * Per-appset defaults for applications
    * Configuring a group of applications (e.g. like roles in app-of-apps)
        Solution1: Have a pool of shared values files?

Next steps:
    Refactor tenant chart into a simplified version - no application extension files
        All "extension" are just updates to the application-template.yaml file (in normal helm way)
        The "template-self" extension can go at the top as a block
    ^^^ also no appset extensions
        generatorsObject and goTemplateOptionsObject can just be updates to the chart
        maybe put the x-git generator in a helper, unsure.

improvements:
    application extensions
        deletion - not important - don't implement? Could implement a finalizersObject instead?
        info-object - not important - don't implement
        sources-object - important
        sync-options-object - not important - don't implement?
        template-self - important (covered by "templating self") - works the way it does because there's no tpl function
    appset extensions
        generatorsObject - important
        goTemplateOptionsObject - not important
        x-git generator - important
    templating self
        introduce concept of metadata
        include propagation of resource ID, so it can be used
    additional resources
        appprojects
        namespaces
        extra-resources
    additional per-namespace resources        
        extra-resources
        limit-ranges
        namespaces
        network-policies
        resource-quotas

Break up the docs into a User Guide + Developer Guide
    also write docs on deployment (incl. use of root app)

