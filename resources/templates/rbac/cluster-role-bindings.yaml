{{- range include "resources.list" (dict
  "root" $
  "values" .Values.clusterRoleBindings
  "defaults" .Values.clusterRoleBindingDefaults
) | fromYamlArray }}
---
{{ include "resources.cluster-role-binding.build" . }}
{{- end }}
