{{- range include "resources.list" (dict
  "root" $
  "values" .Values.clusterRoles
  "defaults" .Values.clusterRoleDefaults
) | fromYamlArray }}
---
{{ include "resources.cluster-role.build" . }}
{{- end }}
