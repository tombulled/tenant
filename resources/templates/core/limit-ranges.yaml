{{- range include "resources.list-with-namespaced" (dict
  "root" $
  "key" "limitRanges"
  "defaults" $.Values.limitRangeDefaults
) | fromYamlArray }}
---
{{ include "resources.limit-range.build" . }}
{{- end }}
