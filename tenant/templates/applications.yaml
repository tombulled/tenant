{{- range include "tenant.resource.list" (dict "root" $ "values" .Values.applications "defaults" .Values.applicationDefaults) | fromYamlArray }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
{{ tpl (include "tenant.application.template" (dict "root" $)) . }}
{{- end }}
