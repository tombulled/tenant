{{- range include "tenant.resources.namespaces" $ | fromYamlArray }}
---
{{ (dict
  "apiVersion" "v1"
  "kind" "Namespace"
  "metadata" (include "tenant.metadata2" (dict "data" . "finalizers" false "namespace" false) | fromYaml)
  "spec" (dict
    "finalizers" .finalizers
  )
) | include "tenant.utils.filter-map" }}
{{- end }}
