{{- range include "tenant.utils.map-to-list" (dict "map" .Values.extraResources) | fromYamlArray }}
---
{{- $value := ternary . (toYaml .) (typeIs "string" .) }}
{{ include "tenant.utils.template" (dict
  "value" $value
  "context" $
  "scope" .
)}}
{{- end }}
