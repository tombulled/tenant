namespaces:
  some-namespace:
    resourceQuotas:
      main:
        name: "{{.id}}"
        annotations:
          namespace: "{{ `{{$namespace.name}}` }}" # <-- works
          namespace2: "{{ `{{.namespace}}` }}"     # <-- works
#           namespace3: "{{$namespace.name}}" # <-- the goal
#           namespace4: "{{.namespace}}"      # <-- the goal
