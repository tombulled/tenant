# defaults:
#   metadata:
#     cluster:
#       id: tom-dev
#       apiHost: "{{.metadata.cluster.id}}.service.tom.lan"
#       url: https://{{.metadata.cluster.apiHost}}:8443/
#       description: "For help on {{.metadata.cluster.id}}, go to {{.metadata.cluster.url}}"
#       aa: foo-{{.metadata.cluster.description}}
#       xx: foo-{{.metadata.cluster.description}}
# 
# applications:
#   foo:
#     annotations:
#       cluster-id: "{{.metadata.cluster.id}}"
#       cluster-apiHost: "{{.metadata.cluster.apiHost}}"
#       cluster-url: "{{.metadata.cluster.url}}"
#       cluster-description: "{{.metadata.cluster.description}}"
#       cluster-aa: "{{.metadata.cluster.aa}}"
#       cluster-xx: "{{.metadata.cluster.xx}}"

# projectDefaults:
#   name: "{{.id}}"
#   rolesObject:
#     read-apps:
#       description: Read-apps privileges for {{.name}}
#       policies:
#         - p, proj:{{.name}}:read-apps, applications, get, {{.name}}/*, allow
#         - p, proj:{{.name}}:read-apps, applicationsets, get, {{.name}}/*, allow

# projects:
#   main:
#     id: main
#     name: "{{.id}}"
#     annotations:
#       my-name-is: "{{.name}}"
#     rolesObject:
#       read-apps:
#         description: Read-apps privileges for {{.name}}
#         policies:
#           - p, proj:{{.name}}:read-apps, applications, get, {{.name}}/*, allow
#           - p, proj:{{.name}}:read-apps, applicationsets, get, {{.name}}/*, allow

# namespaces:
#   foo:
#     annotations:
#       my-name-is: "{{.name}}"
# 
# applications:
#   foo:
#     annotations:
#       my-name-is: "{{.name}}"

applicationSets:
  foo:
    generators:
      - list:
          elements:
            - id: foo
              name: "runner-{{ `{{.id}}` }}"
              annotations:
                my-name-is: "{{ `{{.name}}` }}"
