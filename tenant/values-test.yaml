applicationSetDefaults:
  applyNestedSelectors: false
  generators:
    - list:
        elements:
          - name: foo
  goTemplate: true
  ignoreApplicationDifferences:
    - {name: doo}
  preservedFields:
    foo: bar
  strategy:
    foo: bar
  syncPolicy:
    foo: bar
  template:
    foo: bar
  templatePatch: |
    some
    patch

applicationSets:
  foo: {}
  bar: {}
