namespaceDefaults:
  resources:
    application:
      name: "namespace-metrics-{{.namespace}}"
    extraResource:
      apiVersion: v1
      kind: Extra
      metadata:
        name: "extra-{{.namespace}}"

namespaces:
  foo: {}
  bar: {}
