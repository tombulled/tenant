namespaceDefaults:
  resources:
    limitRanges:
      main:
        name: some-limit-range
        annotations:
          my-id-is: "{{.id}}"
          my-name-is: "{{.name}}"
          namespace-id-is: "{{$namespace.id}}"
          namespace-name-is: "{{$namespace.name}}"

namespaces:
  foo:
    name: fooooooooo
  bar:
    name: barrrrrrrr
