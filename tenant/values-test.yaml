defaults:
  annotations:
    tenant: some-tenant

roleDefaults:
  annotations:
    is-role: "true"

roles:
  foo: {}
  bar: {}

namespaceDefaults:
   annotations:
     is-namespace: "true"

   resources:
     limitRange:
       name: "{{.namespace}}"
       limits:
         - foo

namespaces:
  ns1: {}
  ns2: {}
