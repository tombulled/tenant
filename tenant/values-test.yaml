applicationSet:
  name: my-apps
  goTemplate: true
  template:
    metadata:
      name: "{{ `{{.name}}` }}"
      annotations:
        data: "{{ `{{. | toJson}}` }}"
    spec:
      project: default
  generators:
    - matrix:
        generators:
          - list:
              elements:
                - values: []
          - list:
              elements:
                - name: foo
                - name: bar
                - name: baz
#                   baz-data: "{{ `{{. | toJson}}` }}"
#                   a: "{{ `{{ $_ := set . \"values\" (append .values .) }}` }}"
#     - merge:
#         mergeKeys:
#           - name
#         generators:
#           - list:
#               elements:
#                 - name: foo
#                   age: 12
#                   base-data: "{{ `{{. | toJson}}` }}"
#           - list:
#               elements:
#                 - name: foo
#                   foo-data: "{{ `{{. | toJson}}` }}"
#                 - name: bar
#                 - name: baz
