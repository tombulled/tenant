# Core Concepts

Below are some core concepts of the `tenant` chart:

* **Tenant** A logical grouping of resources within a Kubernetes cluster
* **Resource** A Kubernetes resource type, e.g. `Namespace`, `Pod`, etc.
* **Manifest** YAML representation of a Kubernetes resource
* **Resource Data** The object the `tenant` chart will use to construct a resource manifest
